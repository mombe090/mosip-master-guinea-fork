apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: isory-dev

rules:
  - apiGroups:
      - rbac.authorization.k8s.io
    resources:
      - pods
    verbs:
      - get
      - watch
      - list
      - delete
